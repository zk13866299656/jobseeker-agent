# 《面向软工应届生-求职规划智能Agent系统》软件需求规格说明书(SRS)

## 1. 项目概述

### 1.1 项目背景
当前市面上绝大多数 AI 问答项目均为通用 RAG 知识库 Demo，仅实现文档检索+简单问答，无真实业务流程、无 Agent 自主决策能力、无工具编排能力，同质化严重，简历辨识度极低。

本项目面向**软件工程应届生求职场景**，构建一套**纯 Java 自研调度的智能 Agent 系统**，不依赖 SpringAI 封装黑盒，自主实现 Agent 思维链、任务拆解、多工具调用、长短期记忆、对话压缩、自我反思修正能力。

系统定位：**应届生全流程求职智能工作流助手**，实现自然语言指令 → Agent 自主规划 → 多工具串联执行 → 结果复盘优化的完整闭环。

### 1.2 项目目标
- 构建**可扩展插件式 Agent 工具架构**，新增业务工具无需修改核心调度代码
- 实现复杂自然语言指令的**自动任务拆解、多步执行、失败重试**
- 搭建垂直求职领域 RAG 知识库，降低大模型幻觉
- 实现会话记忆、长文本 Token 压缩、结果反思修正
- 完整工程化落地：异常处理、日志、接口文档、容器部署

### 1.3 用户角色
- **普通用户（应届生）**：发起求职咨询、学习规划、简历优化、面试复盘、岗位筛选
- **管理员**：知识库管理、工具配置、会话数据统计、模型参数配置

### 1.4 项目差异化（简历核心亮点、区别于烂大街项目）
- 拒绝封装框架黑盒：**自研 Agent 主循环调度**，手动实现 CoT 思维链、工具选择、多轮执行
- 非通用知识库：垂直软工应届生求职业务，有完整工作流，不是文档问答
- 具备**反思 Agent**：任务执行完成后自动校验结果、二次优化修正
- 自研**对话上下文压缩算法**，解决长会话 Token 溢出问题
- 插件化工具体系：开闭原则，高可扩展性，符合工程规范

## 2. 整体系统架构需求（强制开发约束）

技术栈严格锁定，不允许随意替换：
- 后端主体：SpringBoot3 + Maven + Java17
- 持久层：MySQL + MyBatis-Plus
- 缓存/短期记忆：Redis
- 向量检索：PgVector / RedisVector（二选一，轻量化优先）
- 大模型调用：DeepSeek/通义千问 Java SDK（仅调用 API，业务逻辑全部自研）
- 工程组件：Lombok、Knife4j、全局异常处理器、统一返回体、日志框架
- 部署：Docker 单机部署

### 架构分层强制要求
前端对话层 → 控制器层 → **Agent 核心调度层（自研）** → 工具插件层 → 数据持久层

## 3. 核心能力整体需求
系统必须支持用户以**任意自然语言**下发复杂求职指令，Agent 自主完成：
1. 意图识别、任务拆解
2. 自动选择所需工具、编排执行顺序
3. 多轮工具调用、中间结果缓存
4. 上下文记忆维护、超长对话压缩
5. 执行结果反思、纠错优化
6. 返回结构化答案+执行过程日志

## 4. 功能需求（详细、可直接开发）

### 4.1 Agent 核心引擎模块（重中之重）

#### 4.1.1 自研 Agent 主循环调度
- 不允许使用 SpringAI、LangChain 封装好的 Agent 调度
- 手动实现 while 循环驱动：思考→选工具→执行→汇总→判断是否继续执行
- 支持多步骤复杂任务自动拆分、分步执行
- 支持工具调用失败重试、异常熔断

#### 4.1.2 思维链 CoT 解析模块
- 让模型输出结构化思考过程，系统解析思考内容
- 区分：用户提问、Agent 思考、工具调用、最终结果

#### 4.1.3 会话记忆系统
- 短期记忆：Redis 存储当前会话最近 10 轮对话
- 长期记忆：MySQL 持久化用户求职短板、学习记录、投递记录
- 记忆自动关联：新提问自动匹配用户历史信息

#### 4.1.4 上下文压缩模块（核心加分项）
- 对话轮次过多/Token 超标时，自动摘要历史对话
- 保留关键信息，剔除冗余重复内容
- 解决长会话溢出、响应变慢问题

#### 4.1.5 反思修正子 Agent
- 单次任务执行完毕后，自动触发反思校验
- 校验维度：答案完整性、准确性、是否贴合用户场景、是否存在幻觉
- 存在缺陷则自动二次优化输出

### 4.2 插件式工具体系（可无限扩展）
统一工具接口规范：包含工具名称、工具描述、入参定义、执行方法

首期必须实现 6 个核心求职工具：

#### 工具1：岗位智能筛选工具
- 功能：根据用户城市、专业、应届生身份、岗位类型，筛选适配国企/互联网岗位
- 输出：岗位名称、硬性条件、技能要求、适配度评分、投递建议

#### 工具2：简历智能优化工具
- 功能：接收用户简历文本，针对 Java/AI 项目、实习经历进行润色、提炼亮点
- 自动规避应届生常见简历问题

#### 工具3：学习计划生成工具
- 根据用户薄弱点、目标岗位、剩余时间，自动生成日/周学习计划
- 绑定 Java 核心考点、Agent 项目优化点、面试题库

#### 工具4：面试题检索与出题工具
- 检索 Java 基础、SpringBoot、Redis、AI Agent、RAG 高频面试题
- 支持针对性出题、知识点讲解

#### 工具5：错题&amp;薄弱点复盘工具
- 记录用户答错/不熟知识点
- 长期记忆沉淀，后续计划自动补强薄弱项

#### 工具6：求职文件导出工具
- 支持导出学习计划表、岗位清单、面试复盘报告为文本/PDF

### 4.3 垂直求职 RAG 知识库模块
- 知识库内容：软工应届生国企岗位要求、Java 面试八股、AI 应用开发面试点、安徽/武汉求职政策、应届生常见问题
- 支持文档切片、向量化入库、相似度检索
- Agent 回答必须**引用知识库内容**，抑制幻觉
- 支持管理员后台上传、更新知识库

### 4.4 会话管理模块
- 会话创建、保存、历史记录查询
- 每一次 Agent 工具调用、思考过程、结果全部留痕
- 支持会话重放，复盘 Agent 决策链路

## 5. 非功能需求（硬性约束）

### 5.1 性能需求
- 普通问答响应 ≤ 2s
- 多工具复杂任务响应 ≤ 8s
- 向量检索耗时 ≤ 500ms

### 5.2 稳定性需求
- 模型调用失败自动重试 2 次，失败熔断友好提示
- 全局异常捕获，无 500 裸异常抛出

### 5.3 可扩展性需求
- 新增工具只需实现统一 Tool 接口，不改动核心调度代码
- 模型、向量库可配置化，支持动态切换

### 5.4 工程化需求
- 统一 RESTful 接口返回格式
- 完整接口文档（Knife4j）
- 关键操作日志记录
- 支持 Docker 一键打包部署

## 6. 数据库核心表结构需求
必须包含 5 张核心表：
1. 用户表：用户基础信息、求职目标、擅长/薄弱技术点
2. 会话表：会话标题、创建时间、用户ID
3. 对话记录表：每轮提问、Agent思考、工具调用记录、返回结果
4. 知识库文档表：文档内容、切片向量、上传时间
5. 错题/薄弱点记录表：知识点、错误次数、掌握度

## 7. 核心业务流程（Trae 必须严格按照此流程开发）

### 7.1 标准 Agent 执行流程
用户输入自然语言指令 → 拼接历史记忆 → Agent 任务拆解 &amp; 思维链思考 → 判定所需工具 → 依次调用工具、缓存中间结果 → 聚合结果 → 反思修正 → 压缩上下文（如需） → 返回最终答案 &amp; 执行日志

## 8. 验收标准（每一条均可自测）
- 能够正常接入大模型，完成基础问答
- 复杂多步骤指令可自动拆解、分步执行多工具
- 会话多轮记忆有效，能关联历史对话
- 超长对话自动压缩，不溢出、不卡顿
- 任务完成后可自动反思优化结果
- 知识库检索有效，答案可溯源、降低幻觉
- 所有工具插件化、可插拔扩展
- 项目可 Docker 正常打包启动

## 9. 项目产出物（最终交付）
- 完整 SpringBoot + Agent 后端源码
- 数据库 SQL 脚本
- Docker 部署脚本
- 接口完整文档
- 项目架构说明文档
- 可直接用于简历的项目亮点说明

## 10. 项目排他约束（防烂大街核心）
### 禁止项：
- 禁止使用 SpringAI 自动 Agent 调度
- 禁止仅做简单 RAG 文档问答 Demo
- 禁止无任务拆解、无反思、无记忆压缩的低质量实现
- 禁止业务场景泛化、无落地工作流